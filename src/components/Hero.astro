<section class="hero">
  <!-- VIDEO DE FONDO -->
  <video
    class="hero-video"
    autoplay
    muted
    loop
    playsinline
    poster="/logo-grande.jpg"
  >
    <source src="/videos/hero.mp4" type="video/mp4" />
  </video>

  <!-- OVERLAY (Se oscurece un poco al hacer hover para leer mejor) -->
  <div class="hero-overlay"></div>

  <!-- CONTENIDO (Oculto por defecto) -->
  <div class="hero-content">
    <div class="hero-content-inner">
      <p class="hero-eyebrow">Sabokar</p>

      <h1 class="hero-title">
        Ingeniería que se transforma <br />
        en <span>acero inoxidable</span>
      </h1>

      

      <div class="hero-actions">
        <!-- BOTONES CONECTADOS -->
        <a href="/Productos" class="btn-primary">
          Ver productos
        </a>

        <a href="/Contacto" class="btn-secondary">
          Contáctanos
        </a>
      </div>
    </div>
  </div>

  <!-- INDICADOR SUTIL (Para que el usuario sepa que hay contenido) -->
  <div class="scroll-indicator">
    <span>Explorar</span>
    <div class="mouse-line"></div>
  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;1,500&family=Montserrat:wght@300;400;600&display=swap');

  :root {
    --p-red: #a4142c;   /* Color 1 */
    --p-pink: #dc8c9c;  /* Color 2 */
    --p-beige: #caa9a8; /* Color 5 */
    --white: #ffffff;
  }

  .hero {
    position: relative;
    height: 100vh;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-color: #000;
  }

  /* --- VIDEO --- */
  .hero-video {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    min-width: 100%;
    min-height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  /* --- OVERLAY --- */
  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.2); /* Muy transparente al inicio */
    transition: background 0.8s ease;
    z-index: 1;
  }

  .hero:hover .hero-overlay {
    background: rgba(0, 0, 0, 0.5); /* Se oscurece al pasar el cursor */
  }

  /* --- CONTENIDO CON EFECTO REVEAL --- */
  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: var(--white);
    max-width: 900px;
    padding: 0 20px;
    
    /* EFECTO DE OCULTAR */
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.2, 0.6, 0.2, 1);
  }

  /* ACTIVACIÓN AL HOVER */
  .hero:hover .hero-content {
    opacity: 1;
    transform: translateY(0);
  }

  .hero-eyebrow {
    font-family: 'Montserrat', sans-serif;
    text-transform: uppercase;
    letter-spacing: 8px;
    font-size: 0.8rem;
    margin-bottom: 20px;
    color: var(--p-beige);
  }

  .hero-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 400;
    line-height: 1.1;
    margin-bottom: 25px;
  }

  .hero-title span {
    font-style: italic;
    color: var(--p-pink);
  }

  .hero-description {
    font-family: 'Montserrat', sans-serif;
    font-size: 1.1rem;
    font-weight: 300;
    max-width: 600px;
    margin: 0 auto 40px;
    opacity: 0.9;
  }

  /* --- BOTONES --- */
  .hero-actions {
    display: flex;
    gap: 20px;
    justify-content: center;
  }

  .btn-primary, .btn-secondary {
    text-decoration: none;
    padding: 15px 35px;
    font-family: 'Montserrat', sans-serif;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-weight: 600;
    transition: all 0.4s ease;
  }

  .btn-primary {
    background-color: var(--p-red);
    color: var(--white);
    border: 1px solid var(--p-red);
  }

  .btn-primary:hover {
    background-color: transparent;
    color: var(--p-red);
  }

  .btn-secondary {
    border: 1px solid var(--white);
    color: var(--white);
  }

  .btn-secondary:hover {
    background-color: var(--white);
    color: var(--p-red);
  }

  /* --- INDICADOR INFERIOR --- */
  .scroll-indicator {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 2;
    text-align: center;
    transition: opacity 0.5s ease;
  }

  /* Desaparece el indicador al hacer hover */
  .hero:hover .scroll-indicator {
    opacity: 0;
  }

  .scroll-indicator span {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 4px;
    color: var(--p-beige);
    display: block;
    margin-bottom: 10px;
  }

  .mouse-line {
    width: 1px;
    height: 50px;
    background: linear-gradient(to bottom, var(--p-beige), transparent);
    margin: 0 auto;
    animation: pulseLine 2s infinite;
  }

  @keyframes pulseLine {
    0% { transform: scaleY(0.5); opacity: 0; }
    50% { transform: scaleY(1); opacity: 1; }
    100% { transform: scaleY(0.5); opacity: 0; }
  }

  @media (max-width: 768px) {
    /* En móvil, que el contenido sea visible o se active al toque */
    .hero-content {
      opacity: 1;
      transform: translateY(0);
    }
    .hero-title { font-size: 2.2rem; }
    .hero-actions { flex-direction: column; gap: 10px; }
  }
</style>