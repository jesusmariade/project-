---
const { contactInfo = {
  phone: "+52 (55) 1234-5678",
  email: "contacto@ingenieria.com",
  businessHours: { weekdays: "Lunes a Viernes: 8:00 AM - 6:00 PM" },
  location: "Parque Industrial Delta, Bodega 12, Ciudad de México"
} } = Astro.props;
---

<section class="contact-hero">
  <div class="hero-video-container">
    <div class="video-overlay"></div>
    <video autoplay muted loop playsinline class="bg-video">
      <source src="https://assets.mixkit.co/videos/preview/mixkit-welding-worker-working-on-metal-4011-large.mp4" type="video/mp4" />
    </video>
  </div>

  <div class="hero-container">
    <div class="hero-content">
      <span class="hero-subtitle-top">Excelencia y Precisión</span>
      
      <h1 class="hero-title">
        Ingeniería en <span class="text-serif">Acero</span> 
        <br /> 
        <span class="text-accent">al siguiente nivel</span>
      </h1>
      
      <p class="hero-description">
        Diseño y fabricación de estructuras industriales con el refinamiento técnico que su proyecto merece.
      </p>
      
      <div class="hero-stats">
        <div class="stat-item">
          <span class="stat-number">10+</span>
          <span class="stat-label">Años</span>
        </div>
        <div class="stat-item">
          <span class="stat-number">150+</span>
          <span class="stat-label">Proyectos</span>
        </div>
        <div class="stat-item">
          <span class="stat-number">50+</span>
          <span class="stat-label">Clientes</span>
        </div>
      </div>
    </div>
  </div>
</section>

<main class="contact-main">
  <div class="contact-container">
    <div class="contact-grid">
      
      <div class="form-card">
        <div class="card-header">
          <h2 class="text-serif">Solicitar Presupuesto</h2>
          <p>Complete el formulario y un asesor técnico se pondrá en contacto con usted.</p>
        </div>
        
        <form class="styled-form">
          <div class="input-row">
            <div class="input-group">
              <input type="text" id="name" placeholder=" " required />
              <label for="name">Nombre completo</label>
            </div>
            <div class="input-group">
              <input type="email" id="email" placeholder=" " required />
              <label for="email">Correo electrónico</label>
            </div>
          </div>

          <div class="input-row">
            <div class="input-group">
              <input type="tel" id="phone" placeholder=" " />
              <label for="phone">Teléfono de contacto</label>
            </div>
            <div class="input-group">
              <select id="service" required>
                <option value="" disabled selected></option>
                <option value="cotizacion">Cotización de equipo</option>
                <option value="personalizado">Proyecto especial</option>
                <option value="soporte">Soporte Técnico</option>
              </select>
              <label for="service">Servicio de interés</label>
            </div>
          </div>

          <div class="input-group">
            <textarea id="message" rows="4" placeholder=" "></textarea>
            <label for="message">Detalles del requerimiento</label>
          </div>

          <button type="submit" class="pandora-button">
            <span>Enviar Solicitud</span>
          </button>
        </form>
      </div>

      <div class="info-sidebar">
        <div class="info-section">
          <h3 class="info-title">Atención Directa</h3>
          <div class="info-item">
            <span class="info-label">Teléfono</span>
            <p class="info-value">{contactInfo.phone}</p>
          </div>
          <div class="info-item">
            <span class="info-label">Email</span>
            <p class="info-value">{contactInfo.email}</p>
          </div>
          <div class="info-item">
            <span class="info-label">Horario</span>
            <p class="info-value">{contactInfo.businessHours.weekdays}</p>
          </div>
        </div>

        <div class="location-card">
          <div class="location-header">
            <h3 class="info-title">Ubicación</h3>
            <p>{contactInfo.location}</p>
          </div>
          <div class="map-container">
            <img src="https://images.unsplash.com/photo-1524661135-423995f22d0b?q=80&w=600&auto=format&fit=crop" alt="Planta" />
            <a href="#" class="map-link">Ver en Google Maps</a>
          </div>
        </div>
      </div>

    </div>
  </div>
</main>

<style>
  :root {
    --primary: #a4142c;
    --dark: #1a1a1a;
    --light-grey: #f4f4f4;
    --white: #ffffff;
    --serif-font: 'Playfair Display', serif; /* Pandora usa fuentes serif con clase */
  }

  /* --- HERO ESTILO LUJO --- */
  .contact-hero {
    position: relative;
    height: 70vh;
    display: flex;
    align-items: center;
    background: #000;
    color: var(--white);
    padding: 0 5%;
  }

  .hero-video-container {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
  }

  .bg-video {
    width: 100%; height: 100%;
    object-fit: cover;
    filter: brightness(0.4);
  }

  .video-overlay {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background: linear-gradient(to right, rgba(0,0,0,0.8) 20%, transparent 100%);
  }

  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
  }

  .hero-subtitle-top {
    text-transform: uppercase;
    letter-spacing: 5px;
    font-size: 0.75rem;
    color: var(--primary);
    margin-bottom: 20px;
    display: block;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 300;
    line-height: 1.1;
    margin-bottom: 30px;
  }

  .text-serif {
    font-family: var(--serif-font);
    font-style: italic;
  }

  .text-accent { color: var(--white); font-weight: 600; }

  .hero-description {
    font-size: 1.1rem;
    opacity: 0.8;
    max-width: 500px;
    margin-bottom: 40px;
    font-weight: 300;
    line-height: 1.6;
  }

  .hero-stats {
    display: flex;
    gap: 40px;
    border-top: 1px solid rgba(255,255,255,0.2);
    padding-top: 30px;
  }

  .stat-number { font-size: 2rem; font-weight: 300; display: block; }
  .stat-label { font-size: 0.7rem; text-transform: uppercase; letter-spacing: 2px; opacity: 0.6; }

  /* --- MAIN CONTENT --- */
  .contact-main { background: var(--white); padding: 80px 0; }
  .contact-container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
  
  .contact-grid { 
    display: grid; 
    grid-template-columns: 1.5fr 1fr; 
    gap: 80px; 
  }

  /* --- FORMULARIO MINIMALISTA (FLOATING LABELS) --- */
  .form-card { background: var(--white); }
  .card-header h2 { font-size: 2.5rem; font-weight: 300; margin-bottom: 15px; }
  .card-header p { color: #666; margin-bottom: 50px; }

  .styled-form { display: flex; flex-direction: column; gap: 40px; }
  .input-row { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; }

  .input-group { position: relative; border-bottom: 1px solid #ccc; transition: 0.3s; }
  .input-group:focus-within { border-bottom-color: var(--primary); }

  .input-group input, .input-group select, .input-group textarea {
    width: 100%;
    padding: 10px 0;
    border: none;
    background: transparent;
    font-size: 1rem;
    outline: none;
  }

  .input-group label {
    position: absolute;
    top: 10px; left: 0;
    color: #999;
    pointer-events: none;
    transition: 0.3s ease all;
    text-transform: uppercase;
    font-size: 0.75rem;
    letter-spacing: 1px;
  }

  /* Efecto de etiqueta flotante */
  .input-group input:focus ~ label,
  .input-group input:not(:placeholder-shown) ~ label,
  .input-group textarea:focus ~ label,
  .input-group textarea:not(:placeholder-shown) ~ label,
  .input-group select:focus ~ label,
  .input-group select:valid ~ label {
    top: -15px;
    font-size: 0.65rem;
    color: var(--primary);
  }

  .pandora-button {
    align-self: flex-start;
    padding: 18px 60px;
    background: var(--primary);
    color: white;
    border: 1px solid var(--primary);
    text-transform: uppercase;
    letter-spacing: 3px;
    font-size: 0.8rem;
    cursor: pointer;
    transition: 0.4s;
  }

  .pandora-button:hover {
    background: transparent;
    color: var(--primary);
  }

  /* --- SIDEBAR --- */
  .info-sidebar { display: flex; flex-direction: column; gap: 60px; }

  .info-title {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: var(--primary);
    margin-bottom: 25px;
    font-weight: 700;
  }

  .info-item { margin-bottom: 25px; }
  .info-label { font-size: 0.7rem; text-transform: uppercase; color: #999; display: block; margin-bottom: 5px; }
  .info-value { font-size: 1.1rem; color: var(--dark); font-weight: 400; }

  .map-container {
    position: relative;
    height: 200px;
    overflow: hidden;
  }

  .map-container img { width: 100%; height: 100%; object-fit: cover; filter: grayscale(1); }
  
  .map-link {
    position: absolute;
    bottom: 0; left: 0; right: 0;
    background: rgba(26,26,26,0.9);
    color: white;
    text-align: center;
    padding: 10px;
    text-decoration: none;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: 0.3s;
  }

  .map-link:hover { background: var(--primary); }

  /* --- RESPONSIVE --- */
  @media (max-width: 900px) {
    .contact-grid { grid-template-columns: 1fr; gap: 60px; }
    .hero-stats { gap: 20px; }
    .input-row { grid-template-columns: 1fr; }
  }
</style>