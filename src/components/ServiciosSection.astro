<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Servicios Estilo Pandora</title>
    <!-- Tipografías de lujo -->
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;600&family=Montserrat:wght@200;300;400&display=swap" rel="stylesheet">
   
</head>
<body>

    <!-- SECCIÓN 1 (Lo que hacemos) -->
    <section id="servicios" class="servicios-section">
        <div class="container">
            <div class="section-header">
                <p class="servicios-subtitle">Soluciones integrales para tu industria</p>
                <h2 class="servicios-title">Lo que hacemos</h2>
            </div>

            <div class="servicios-grid">
                <!-- Servicio 1 -->
                <div class="servicio-card">
                    <div class="servicio-bg-circle"></div>
                    <div class="servicio-icon">
                        <svg width="48" height="48" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                        </svg>
                    </div>
                    <h3 class="servicio-titulo">Diseño Industrial</h3>
                    <p class="servicio-descripcion">Modelado y desarrollo de maquinaria en acero inoxidable optimizada.</p>
                </div>

                <!-- Servicio 2 -->
                <div class="servicio-card">
                    <div class="servicio-bg-circle"></div>
                    <div class="servicio-icon">
                        <svg width="48" height="48" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547"/>
                        </svg>
                    </div>
                    <h3 class="servicio-titulo">Fabricación</h3>
                    <p class="servicio-descripcion">Manufactura precisa con altos estándares de calidad y acabados profesionales.</p>
                </div>

                <!-- Servicio 3 -->
                <div class="servicio-card">
                    <div class="servicio-bg-circle"></div>
                    <div class="servicio-icon">
                        <svg width="48" height="48" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4"/>
                        </svg>
                    </div>
                    <h3 class="servicio-titulo">Soluciones a Medida</h3>
                    <p class="servicio-descripcion">Cada proyecto es único. Adaptamos cada máquina a tus necesidades.</p>
                </div>
            </div>
        </div>
    </section>

    <div class="spacer"></div>

    <!-- SECCIÓN 2 (Nuestros Servicios) -->
    <section class="servicios-section">
        <div class="container">
            <div class="section-header">
                <span class="section-subtitle">Lo que ofrecemos</span>
                <h2 class="section-title">Nuestros Servicios</h2>
            </div>
            
            <div class="servicios-grid">
                <!-- Card 1 -->
                <div class="servicio-card">
                    <div class="servicio-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
                        </svg>
                    </div>
                    <h3>Diseño Personalizado</h3>
                    <p>Equipos adaptados a tus necesidades específicas de producción.</p>
                </div>
                
                <!-- Card 2 -->
                <div class="servicio-card">
                    <div class="servicio-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                        </svg>
                    </div>
                    <h3>Asesoría Técnica</h3>
                    <p>Expertos en procesos industriales para optimizar tu producción.</p>
                </div>
                
                <!-- Card 3 -->
                <div class="servicio-card">
                    <div class="servicio-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
                            <path d="M3 9h18M9 21V9"/>
                        </svg>
                    </div>
                    <h3>Mantenimiento</h3>
                    <p>Servicio post-venta y mantenimiento preventivo garantizado.</p>
                </div>
                
                <!-- Card 4 -->
                <div class="servicio-card">
                    <div class="servicio-icon">
                        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                            <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                        </svg>
                    </div>
                    <h3>Entrega Rápida</h3>
                    <p>Logística eficiente para entregas puntuales en todo el país.</p>
                </div>
            </div>
        </div>
    </section>

</body>
</html>

 <style>
        :root {
            --color1: #a4142c; /* Rojo Profundo */
            --color2: #dc8c9c; /* Rosa Suave */
            --color3: #a30c27; /* Rojo Intenso */
            --color4: #b95d68; /* Rosa Viejo */
            --color5: #caa9a8; /* Beige Rosado */
            --white: #ffffff;
            --bg-light: #fffafb;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background-color: var(--white);
            color: #333;
            line-height: 1.6;
        }

        /* --- ESTILOS GENERALES DE SECCIÓN --- */
        .servicios-section {
            padding: 100px 20px;
            background-color: var(--bg-light);
            text-align: center;
            border-bottom: 1px solid rgba(202, 169, 168, 0.3);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-header {
            margin-bottom: 70px;
        }

        .section-subtitle, .servicios-subtitle {
            display: block;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 5px;
            color: var(--color4);
            margin-bottom: 15px;
            font-weight: 400;
        }

        .section-title, .servicios-title {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3.2rem;
            color: var(--color1);
            font-weight: 300;
            margin-bottom: 20px;
        }

        /* Línea decorativa estilo joyería */
        .section-title::after {
            content: '';
            display: block;
            width: 50px;
            height: 1px;
            background: var(--color2);
            margin: 20px auto 0;
        }

        /* --- GRID Y TARJETAS --- */
        .servicios-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
            padding: 10px;
        }

        .servicio-card {
            background: var(--white);
            padding: 50px 35px;
            position: relative;
            transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
            border: 1px solid rgba(202, 169, 168, 0.2);
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .servicio-card:hover {
            transform: translateY(-10px);
            border-color: var(--color2);
            box-shadow: 0 20px 40px rgba(185, 93, 104, 0.08);
        }

        /* Íconos Estilizados */
        .servicio-icon {
            margin-bottom: 30px;
            color: var(--color3);
            transition: transform 0.5s ease;
        }

        .servicio-card:hover .servicio-icon {
            transform: scale(1.1) rotateY(10deg);
        }

        .servicio-icon svg {
            stroke-width: 1.2px; /* Líneas finas estilo Pandora */
        }

        /* Tipografía de Tarjetas */
        h3, .servicio-titulo {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.8rem;
            color: var(--color1);
            margin-bottom: 18px;
            font-weight: 500;
        }

        p, .servicio-descripcion {
            font-size: 0.95rem;
            color: #777;
            font-weight: 300;
            max-width: 250px;
            margin: 0 auto;
        }

        /* Detalle circular decorativo (Segunda Sección) */
        .servicio-bg-circle {
            position: absolute;
            top: 0;
            right: 0;
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--color5) 0%, transparent 70%);
            opacity: 0.15;
            border-radius: 0 0 0 100%;
            transition: all 0.5s ease;
        }

        .servicio-card:hover .servicio-bg-circle {
            width: 100%;
            height: 100%;
            border-radius: 0;
            opacity: 0.05;
        }

        /* Separador entre secciones */
        .spacer {
            height: 50px;
            background-color: var(--white);
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            .section-title, .servicios-title {
                font-size: 2.5rem;
            }
            .servicios-section {
                padding: 60px 15px;
            }
        }
    </style>